/**
 * Calculate the mean of an array of integers.
 */
#pragma once

#include <stdlib.h>

/**
 * Calculate the mean of an array of integers.
 *
 * @param array The list of integers to take the mean of.
 * @param array_len The number of integers in array.
 * @return The mean value of the array.
 */
float mean(const int array[], size_t array_len);
