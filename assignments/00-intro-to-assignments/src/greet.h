/**
 * Personalized greeting printer.
 */
#pragma once

/**
 * Print a personalized greeting.
 *
 * Given a string representing someone's name, print the string "Greetings,
 * <name>!" to standard output (where <name> is replaced by the name provided).
 *
 * @param name The name to print the greeting for.
 */
void greet(const char* name);
