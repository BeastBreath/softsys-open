/**
 * Absolute value utility function.
 */
#pragma once

/**
 * Compute the absolute value of an integer.
 *
 * @param n The input integer to take the absolute value of.
 * @return -n if n was negative and n otherwise.
 */
int absolute_value(int n);
