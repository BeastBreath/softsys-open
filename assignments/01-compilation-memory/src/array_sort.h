#pragma once

#include <stddef.h>

/**
 * Replace this with your documentation comment.
 */
void swap(int* left_ptr, int* right_ptr);

/**
 * Replace this with your documentation comment.
 */
void sort_inplace(int numbers[], size_t array_size);

/**
 * Replace this with your documentation comment.
 */
void sort_copy(const int numbers[], int sorted[], size_t array_size);
