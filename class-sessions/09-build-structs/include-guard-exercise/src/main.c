// Modify this code appropriately to make compilation work.

int main(void) {
  int num = 42;
  int mod = 5;
  printf("%d^2 is %d\n", num, square(num));
  printf("%d^2 mod %d is %d\n", num, mod, square_mod(num, mod));
  return 0;
}
