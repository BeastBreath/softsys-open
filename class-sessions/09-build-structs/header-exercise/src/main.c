// Place the right includes here to make the code work.

int main(void) {
  int dividend = 42;
  int divisor = 5;
  printf("%d / %d is %f\n", dividend, divisor, div(dividend, divisor));
  return 0;
}
